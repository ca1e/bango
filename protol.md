# Gomocup 五子棋 AI 协议（中文对照版）

依据官方协议文档翻译整理（[Gomoku AI protocol](https://plastovicka.github.io/protocl2en.htm)，含 2023-03-20 版本历史）。与官方冲突时以官方为准。

## 一、协议概述

Gomocup 协议（又称 Piskvork 协议）是比赛管理器（Manager）与 AI 引擎（Brain）之间的标准通信协议。引擎通过标准输入（stdin）读取命令、标准输出（stdout）发送回复，因此可用任何语言实现。

协议将 UI 界面、对战调度与 AI 逻辑彻底分离，开发者可专注于 AI 本身。

## 二、通信机制

### 2.1 基本通信方式

- 管理器创建两个管道：一个向引擎发送命令，一个接收引擎回复。
- 引擎必须是控制台应用程序，不能是 GUI 程序。
- 注意运行时的控制台输出缓冲：C 语言中每次 `printf` 后必须 `fflush(stdout)`，否则管理器会一直等待。

### 2.2 行格式

- 每行恰好一条命令（唯一例外：`BOARD` 命令带多行数据）。
- 管理器发送的行以 `CR LF`（0x0d 0x0a）结尾。
- 引擎回复可用 `CR LF`、仅 `LF` 或仅 `CR` 结尾。
- 管理器忽略空行；不会因超长行崩溃，但可能静默截断超长行。

### 2.3 线程与死锁

- 单线程引擎在"思考或响应命令"期间**严禁读取输入**，否则引擎与管理器互相等待造成死锁，管理器会在每步超时后终止引擎。
- 推荐双线程：线程 1 读命令，线程 2 思考并写回复，用同步对象（事件/锁/信号量）协调。
- 线程数对锦标赛不重要，单线程即可参赛；双线程对人类玩家有用（思考中改时限、随时取消思考）。

## 三、引擎命名与文件

- 可执行文件名必须以 `pbrain-` 开头（如 `pbrain-myAI.exe`），否则被视为旧版基于文件的通信方式。
- 名字只能含 `A-Z a-z 0-9 - _ .`。
- ZIP 包名不需要前缀；32/64 位 exe 可同包，64 位文件名须含子串 `64`。
- 工作目录由管理器指定，不一定是 exe 所在目录；引擎访问数据文件须用完整路径。
- 引擎可在当前目录创建与引擎同名的文件夹存临时文件（上限现为 20MB）。`INFO folder` 指示持久文件目录：引擎须在其中建同名子文件夹；未收到 `INFO folder` 则不可存持久文件。

## 四、管理器 → 引擎命令

引擎必须处理：`START`、`BEGIN`、`INFO`、`BOARD`、`TURN`、`END`。

### 4.1 START [size] — 初始化

初始化并创建空棋盘，不落子。棋盘大小参数为 `size`。**引擎必须支持 20 路**（Gomocup 标准赛制），其他尺寸推荐但非必须支持。不接受的尺寸回复 `ERROR`（可附消息），成功回复 `OK`。

### 4.2 TURN [X],[Y] — 对手落子，请求应答

参数为对手落子坐标（坐标从 0 开始编号）。引擎回复自己的落子坐标（两个逗号分隔的数字）。

### 4.3 BEGIN — 引擎先行

告知引擎从空盘开局（先手），引擎立即回复第一步落子。启用自动开局时不使用 `BEGIN`（双方改收 `BOARD`）。

### 4.4 BOARD — 设置完整局面

```
BOARD
[X],[Y],[field]
...（多行）
DONE
```

- 用于恢复对局 / 悔棋（undo/redo）。通常在 `START`、`RESTART`、`RECTSTART` 后棋盘为空时发送；若存在未结束的对局，管理器会先发 `RESTART`。
- `field`：**1** = 己方棋子，**2** = 对方棋子，**3** = 仅连续游戏模式：属于胜利连线的棋子，或按连珠规则为禁点。
- 规则为连珠（renju）时，各行必须**按落子顺序**发送；自由规则（gomoku）下可乱序，引擎须自行处理。
- `DONE` 结束后，引擎按 `TURN`/`BEGIN` 的方式**回复落子**（轮到己方时）。

### 4.5 INFO [key] [value] — 配置信息

引擎可以忽略不需要的 INFO，但超限会判负；且必须容忍管理器不发送下述部分信息。多数信息在开局前发送；锦标赛中时限不会中途改变。建议随时响应 INFO（人类玩家可能在引擎思考中修改）。

| key | 含义 |
| --- | --- |
| `timeout_turn` | 每步时限（毫秒；0 = 尽快落子，如只做静态评估不搜索） |
| `timeout_match` | 整局时限（毫秒；0 = 无限制） |
| `max_memory` | 内存上限（字节；0 = 无限制） |
| `time_left` | 整局剩余时间（毫秒）。每步落子命令（TURN/BEGIN/BOARD/SWAP2BOARD）前发送；可为负（已超时）；无整局限制时为 **2147483647** |
| `game_type` | 0=人类对手，1=AI 对手，2=锦标赛，3=网络锦标赛 |
| `rule` | **位掩码求和**：1=恰五连获胜，2=连续游戏，4=连珠(renju)，8=Caro |
| `evaluate` | 鼠标光标处坐标 X,Y。仅 debug 版应答（不可写 stdout），release 版忽略 |
| `folder` | 持久文件目录（见第三节） |

- 时间测量：整局时间从进程创建到对局结束（对手回合不计）；每步时间含除初始化（START/RECTSTART/RESTART）外的所有命令处理。
- 限时的场合管理器**必须**发 `time_left`，引擎可只依赖它而忽略 `timeout_match`。
- 未知 INFO：忽略。无法满足的 INFO（如内存过小）：**暂缓报错**，等管理器发来第一个非 INFO 命令（TURN/BOARD/BEGIN）时再输出问题——管理器发 INFO 期间不读取引擎输出。
- INFO 无应答。

### 4.6 END — 结束

引擎必须尽快退出（超过约 1 秒会被强杀）。**无应答**，且 END 之后不应再向输出写任何内容。引擎应删除临时文件。

### 4.7 ABOUT — 引擎信息

引擎单行返回自身信息，格式为 `keyword="value"`，推荐 keyword：`name`、`version`、`author`、`country`、`www`、`email`，值间用逗号分隔（逗号后可跟空格）。

```
name="SomeBrain", version="1.0", author="Nymand", country="USA"
```

## 五、可选命令（管理器 → 引擎）

未实现时回复 `UNKNOWN`，管理器会改用必选命令。

- `RECTSTART [width],[height]` — 矩形棋盘（宽对 X，高对 Y）。方阵时管理器必须用 START。成功 `OK`，不支持 `ERROR`。
- `RESTART` — 对局结束/中止后重开：保留棋盘尺寸，清空棋盘重建结构，回复 `OK`，之后通信同 START。若引擎答 UNKNOWN，管理器会发 END 并重启引擎。
- `TAKEBACK [X],[Y]` — 悔棋：移除该坐标棋子，回复 `OK`。
- `PLAY [X],[Y]` — 仅作为管理器对 `SUGGEST` 的应答：把 [X],[Y] 强加给引擎。引擎回复坐标（通常与参数一致；不认同时可回别的坐标，但不推荐）。
- `SWAP2BOARD` — Swap2 规则开局，在 START 与 BOARD 之间发送一或两次，行数据以 `DONE` 结束：
  - **情形 1**（无数据行）：引擎连出前三个点的坐标，如 `7,7 8,7 9,9`；
  - **情形 2**（三个点的坐标）：引擎三选一——`SWAP`（换色）；或回第 4 手坐标（不换）；或回第 4、5 两手坐标（放两子让对方选色）；
  - **情形 3**（五个点的坐标）：二选一——`SWAP`（换色）；或回第 6 手坐标（不换）。
  - 开局阶段结束后，盘面作为标准对局的开局继续（由 BOARD 命令下发双方）。

## 六、引擎 → 管理器回复

- `[X],[Y]` — 落子应答（TURN/BEGIN/BOARD；或 PLAY 的确认）。
- `OK` — 确认（START/RECTSTART/RESTART/TAKEBACK；BOARD 仅在**不轮到己方落子**时）。
- `UNKNOWN [message]` — 未知或未实现命令。收到陌生的行**不得退出**；管理器对未实现的可选命令会改发必选命令。
- `ERROR [message]` — 已知命令但无法执行（如内存过小、棋盘过大）。管理器可写日志、调整选项后重试。
- `MESSAGE [message]` — 给用户的消息，写入日志窗口/文件。只在应答命令之前发送，消息内不得有换行；多行文本拆成多条 MESSAGE。建议只发英文。
- `DEBUG [message]` — 同 MESSAGE，仅调试用，Gomocup 公开赛中不可见。
- `SUGGEST [X],[Y]` — 可代替落子应答，且不改变引擎内部状态。管理器以 PLAY 或 END 应答（锦标赛中管理器总是采纳 SUGGEST 的坐标）。多数引擎不使用。

## 七、典型流程

### 7.1 引擎先手

```
管理器 → 引擎：START 20      引擎 → 管理器：OK
管理器 → 引擎：INFO ...      （无应答）
管理器 → 引擎：BEGIN         引擎 → 管理器：10,10
管理器 → 引擎：TURN 11,10    引擎 → 管理器：9,10
...
管理器 → 引擎：END           （引擎尽快退出，无应答）
```

### 7.2 恢复对局（轮到引擎走）

```
管理器 → 引擎：BOARD
管理器 → 引擎：10,10,1
管理器 → 引擎：10,11,2
管理器 → 引擎：11,11,1
管理器 → 引擎：9,10,2
管理器 → 引擎：DONE
引擎 → 管理器：9,9            （轮到引擎，直接回着法）
```

## 八、要点速查

| 项目 | 说明 |
| --- | --- |
| 通信方式 | stdin / stdout，控制台程序 |
| 行结束符 | 管理器 CR LF；引擎 CR LF / LF / CR 均可 |
| 每行命令数 | 一条（BOARD 数据块除外） |
| 引擎命名 | 可执行文件必须以 `pbrain-` 开头 |
| 必须支持的命令 | START, BEGIN, INFO, BOARD, TURN, END |
| 棋盘尺寸 | **必须支持 20**；不支持的尺寸回 ERROR |
| 坐标 | 从 0 开始编号 |
| BOARD field | 1 己方 / 2 对方 / 3 胜利线标记或禁点（连续游戏） |
| BOARD 完成后 | 轮到己方则回着法；否则（罕见）OK |
| END | 无应答，之后不再输出，尽快退出 |
| INFO time_left | 每步前发送；可为负；无限制时 2147483647 |
| INFO rule | 位掩码：1 恰五连 + 2 连续 + 4 连珠 + 8 Caro |
| 未知命令 | 回 UNKNOWN，不得退出 |
